// START PROGRAM 
// CREATE an array called array to store student data
// id, name, age, class
// DEFINE a function called showtable(curarray)
// create a variable const table and assign it to the get element by id 
// SET table row and id,name,age,class as table data
// IF curarray is empty:
// DISPLAY Message: "No Student Found!" as the error message.
// Hide the table.
// ELSE:
// clear any error messages
// FOR each student in curarray:
// Add a row in the table with that student's data
// Make  the table visible
// call showtable(array) to display all student initially
// SET UP event listener on the search input field
// ON every keyup
// GET the search input text, convert to lowercase, and trim whitespace
// IF search input is empty:
// call showtable(array) to show all student data again
// Exit the function
// CREATE a variable called newarray and Filter the array
// convert each field(id,name,age,class) to string and lowercase
// call function showtable(newarray)
// END PROGRAM